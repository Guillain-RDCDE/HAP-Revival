name: Lint Python tools

on:
  pull_request:
    paths:
      - "tools/**/*.py"
      - "tests/**/*.py"
      - ".github/workflows/python-lint.yml"
  push:
    branches: [main]
    paths:
      - "tools/**/*.py"
      - "tests/**/*.py"
      - ".github/workflows/python-lint.yml"

jobs:
  ruff:
    name: ruff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install ruff
        # Pinned deliberately: unpinned, every ruff release enables new default
        # rules and turns this gate red on code nobody touched. 0.16.5 did
        # exactly that on 2026-08-23 (79 findings, none of them new code).
        # Bump on purpose, with the cleanup in the same commit.
        run: pip install ruff==0.15.14
      - name: Run ruff
        run: ruff check tools/ tests/

  syntax-check:
    name: Python syntax check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Compile all tools
        run: python -m compileall -q tools/

  tests:
    name: pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install pytest
        run: pip install pytest
      - name: Run the test suite
        # Stdlib + pytest only — pure-logic + a loopback mock device. No HAP needed.
        run: python -m pytest tests/ -q
